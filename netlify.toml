[build]
  base = "frontend"
  # BACKEND_ORIGIN must be set in Netlify env before build (writes public/_redirects for API proxy).
  command = "npm ci && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"
