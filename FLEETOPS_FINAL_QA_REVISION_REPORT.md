# FleetOps Final QA Revision Report

**Date:** 2026-07-13  
**Scope:** Final QA per plan — audit-first, panel validation, harness E2E, then targeted fixes only  
**Constraint:** No redesign; preserve booking → payment → dispatch → helper/driver → reports/analytics workflows  

---

## Executive verdict

Core operational path is **production-ready** for panel demo after prior QA fixes plus this pass’s targeted UI/stub fixes. Golden workflow **PASS (0 failed)** post-fix. Toll corridor smoke **5/5 PASS**. Cost formula smoke **PASS**. Payment approval E2E **8/8 PASS**. Module smoke **0 failed** after probe-path alignment. Remaining items are documented tooling/seed gaps—not blockers for the primary thesis workflow.

---

## 1. Already working

| Area | Evidence | Evidence type |
|------|----------|---------------|
| Secure signup + password policy | FE `formValidation.ts` + BE `password_policy.py`; register rejects weak passwords | static + prior auth E2E |
| Name spaces in First/Last | `sanitizeAuthNameInput` / `normalizeAuthName` on sign-up | static |
| Logout confirmation | `SignOutConfirmDialog` via `NavBarAuth` | static |
| Booking wizard steps + validation | `CostCalculator` + `bookingWizardValidation` (route→shipment→schedule→documents→review→pricing→payment) | static |
| Wizard persistence (Previous) | Parent state holds files/signature/fields; `DigitalSignaturePad` restores via `value` | static |
| Cargo description + category required | FE shipment step + BE `BookingCreate` validators; `other` allowed | static + golden #225 |
| Cargo declaration + e-signature + signed terms PDF | `with-documents` + `terms_agreement_pdf.py` | golden create_booking PASS |
| Xendit session/QR/webhook code path | `xendit_payment.py` + payments routes (env-dependent live QR) | static |
| Cash ↔ Xendit coexistence | Cash soft-rejects pending Xendit; Xendit blocked when cash pending; FE `canSubmitCash` / `canStartXendit` | static |
| Payment verify → goods → cargo → dispatch | Golden #225 | API |
| Dispatcher conflict / no duplicate truck-driver-helper | `dispatch_resource_availability.py` + assign-batch 400 + FE validation | static + golden assign |
| Helper unlimited timeline + ✓ milestones + map/structured location | `REQUIRED_EN_ROUTE_LOCATION_UPDATES = 0`, `HelperLocationSelector`, crew checklist ✓ | static |
| Shipment tracker stages + overflow containment | Unique stage IDs; `WorkflowTimeline` wrap/scroll; Sites grid `min-width:0` | static |
| Cost formulas (fuel/labor/shares/cargo/toll) | `cost_estimation_smoke_test.py` PASS | API/unit |
| Toll Class 3 matrix + nearest plazas; miss → ₱0 | Corridor smoke: Balintawak→San Fernando ₱1120; local ₱0 explicit | API |
| Analytics descriptive + predictive + prescriptive APIs | Prior 23/23 analytics E2E; `optimize-route` / what-if / forecasts | prior + static |
| Live CSV reports + PDF metadata helpers | `/api/reports/*.csv`, manager CSVs; `professionalReportPdf.ts` Generated By/Date/Time/Filters | golden + static |
| Role FE dashboards reachability | Module smoke FE routes 200 | API |
| AuthZ denials | Customer/driver/helper denied admin endpoints 403 | module smoke |

---

## 2. Partially working

| ID | Location | Cause | Severity | Recommended fix | Fixed? | DB? | Workflow changed? |
|----|----------|-------|----------|-----------------|--------|-----|-------------------|
| P1 | `EvidenceCaptureInput` + booking doc upload | Panel: uploads felt harder; gallery fallback default true; booking upload is bare file input | Medium | Clarify primary CTA, simplify helper copy; keep same validation | **Yes** — clearer CTAs, ready state, Remove link; camera primary | No | No |
| P2 | Analytics Diagnostic UI | Diagnostic folded into Descriptive in role grids | Low | Document; no UI rewrite | Document only | No | No |
| P3 | `modules/analytics/prescriptive/page.tsx` | Marketing stub; real tools at whatif / route-optimizer | Medium | Redirect to live tools | **Yes** — redirects to `/modules/analytics/route-optimizer` | No | No |
| P4 | Camera-only “where required” | `allowGalleryFallback` defaults true; gallery flagged for review | Low | Keep camera primary; optional tighten at call sites — not blocking | Document (camera remains primary CTA) | No | No |
| P5 | Module smoke wrong probes | `/api/workflow/my-trips`, `/api/customer/bookings` 404 — real paths are `/api/driver/trips`, `/api/bookings` | Low | Align smoke paths | **Yes** — `qa_module_smoke.py` updated | No | No |
| P6 | Manager approve after payment verify | Golden: approve returns 400 (status already payment_verified) | Low | Intentional status machine; UX skip approve | Document | No | No |
| P7 | Live Xendit webhook | Needs public HTTPS + keys; localhost falls back to invoice | Medium (env) | Configure for production; code OK | Env | No | No |
| P8 | Seed goods declaration files | Prior M1 — goods E2E may lack seed declaration rows | Low | Seed sample docs if E2E needed | Out of scope | No* | No |
| P9 | passlib/bcrypt version warning | Package skew; login still works | Low | Pin versions later | Out of scope | No | No |

\*Seed file paths only if later extended—no schema change.

---

## 3. Broken

| ID | Location | Cause | Severity | Recommended fix | Fixed? | DB? | Workflow changed? |
|----|----------|-------|----------|-----------------|--------|-----|-------------------|
| B1 | `/reports/generate`, `/reports/final` | `DriverDataFlowService` localStorage mock — not live API | High (panel) | Redirect to live analytics/CSV report surfaces | **Yes** — redirect to `/modules/analytics/reports` | No | No |
| B2 | `scripts/payment_approval_e2e_test.py` | Reuses any `payment_verification` booking; customer1 gets 403 “Not your booking” | Medium (harness) | Filter by customer1’s bookings or always create as customer1 | **Yes** — owns booking + session reopen | No | No |

No Broken findings in core booking/dispatch/helper/driver/payment product code from this harness pass.

---

## 4. Panel comments matrix

| Panel comment | Status | Touch? |
|---------------|--------|--------|
| Secure signup | Working | No |
| Password policy / requirements / weak passwords | Working | No |
| Logout confirmation | Working | No |
| Xendit + QR + webhook + auto verify | Working (env for live webhook) | No unless regression |
| Cash workflow | Working (guards present) | No unless regression |
| Cargo declaration + e-signature + terms PDF | Working | No |
| Helper uploads unlimited + camera preference | Working / Partial (gallery allowed) | UX ease only |
| Booking wizard persist + review + cost | Working | No |
| Shipment tracker no overflow / current stage | Working | No |
| Dispatcher availability / no dual assign | Working | No |
| Helper ✓ progress + map/structured address | Working | No |
| Reports PDF metadata + live CSV | Working | No |
| `/reports/generate` live | **Broken → Fixed** (redirect) | Yes — redirect |
| Uploads easier | Partial → improved UX | Yes — UX only |

---

## 5. Harness results appendix (2026-07-13)

### Pre-fix
| Harness | Result | Notes |
|---------|--------|-------|
| `scripts/qa_golden_workflow.py` | PASS then intermittent FAIL | Busy helpers/trucks from incomplete prior POD-blocked trips |
| `scripts/qa_module_smoke.py` | 3 FAIL (probe paths) | Wrong endpoint paths |
| `scripts/toll_matrix_corridor_smoke.py` | **5/5 PASS** | Class 3 matrix; local corridor explicit ₱0 |
| `scripts/cost_estimation_smoke_test.py` | **PASS** | Fuel parse + formula lines |
| `scripts/payment_approval_e2e_test.py` | FAIL step 2 | Wrong customer ownership |

### Post-fix re-verify
| Harness | Result | Notes |
|---------|--------|-------|
| `scripts/qa_module_smoke.py` | **0 failed** | Corrected `/api/driver/trips`, `/api/customer/current-bookings` |
| `scripts/payment_approval_e2e_test.py` | **8/8 PASS** | Customer-owned booking |
| `scripts/qa_golden_workflow.py` | **0 failed** | Booking #231; toll ₱1120; trip #218 through en route; complete correctly blocked pending POD; QA env free-stuck for leftover trips/holds/assignments |
| Toll / cost | Unchanged PASS | Not re-broken |

---

## 6. Untouched by design

- Pricing/toll formula engines (`booking_pricing.py`, `toll_matrix.py` Class 3 nearest-geo)
- Dispatch conflict engine
- Xendit online flow (except cash coexistence already present)
- Auth password policy / logout dialog
- Analytics API implementations that already pass
- Excel (.xlsx) export (not implemented — product gap L2)
- Cross-role analytics switcher
- Seed password `password` vs register policy (local seed by design)
- Database schema (no migration required this pass)

---

## 7. Fixes applied this pass

1. **P1 Upload UX** — `BookingDocumentUploadFields.tsx`, `EvidenceCaptureInput.tsx`, signature pad hint copy.  
2. **B1 Reports mock** — `/reports/generate` and `/reports/final` redirect to `/modules/analytics/reports`.  
3. **P3 Prescriptive stub** — redirects to `/modules/analytics/route-optimizer`.  
4. **B2 / P5 Harnesses** — payment E2E ownership; module smoke paths; golden QA env free-stuck (trips, holds, assignments, crew flags) + open-slot search.

No database schema changes. No workflow redesign.
