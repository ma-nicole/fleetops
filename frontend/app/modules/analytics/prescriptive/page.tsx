import { redirect } from "next/navigation";

/**
 * Marketing stub removed — real prescriptive tools are route optimizer + what-if.
 */
export default function PrescriptiveAnalyticsRedirectPage() {
  redirect("/modules/analytics/route-optimizer");
}
