# Picked up automatically by `next dev` (no need to copy to .env.local for defaults).
# Browser → same-origin `/api-proxy/*` → FastAPI `BACKEND_ORIGIN` + `/api/*`.
NEXT_PUBLIC_API_URL=/api-proxy
BACKEND_ORIGIN=http://127.0.0.1:8000
